#!/bin/bash

#DEBHELPER#

if [ "$1" = "remove" ]; then
    # remove the signature cache directory
    rm -r -f /var/lib/bctid//database/*

    # remove the log file
    rm -f /var/log/bctid.log

    # remove the pid file directory
    rm -r -f /var/run/bctid
    
fi

exit 0
