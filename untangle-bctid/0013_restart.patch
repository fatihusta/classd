diff -ur bcti-sdk-linux-5.27.2.orig/src/Daemon/main.cpp bcti-sdk-linux-5.27.2/src/Daemon/main.cpp
--- bcti-sdk-linux-5.27.2.orig/src/Daemon/main.cpp	2019-06-14 11:18:07.000000000 -0400
+++ bcti-sdk-linux-5.27.2/src/Daemon/main.cpp	2019-07-29 14:12:06.000000000 -0400
@@ -99,6 +99,9 @@
 
 static void KillHandler(int sig) { ++s_killDaemon; }
 
+static int BindRes = 0;
+
+
 ConfigData conf;
 
 // < 0 = failure, >= 0 = success
@@ -633,8 +636,8 @@
 	int yes = 1;
 	setsockopt(outPollFd[0].fd, SOL_SOCKET, SO_REUSEADDR, &yes, sizeof yes);
 
-	int bindRes = bind(outPollFd[0].fd, (sockaddr*)&outServerAddr, sizeof outServerAddr);
-	if (bindRes < 0)
+	BindRes = bind(outPollFd[0].fd, (sockaddr*)&outServerAddr, sizeof outServerAddr);
+	if (BindRes < 0)
 	{
 		close(outPollFd[0].fd);
 		return "Binding error, socket unavailable.";
@@ -757,6 +760,7 @@
 	DaemonLoop(opts, serverAddr, pollFd);
 
 	Shutdown();
+	close(BindRes);
 	exit(EXIT_SUCCESS);
 }
 
