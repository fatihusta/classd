#!/bin/dash

PING_HOST=$1

if [ "${FAIL_PRIMARY_ADDRESS}" = "0.0.0.0" ]; then
    ping -I ${FAILD_OS_NAME} -c 1 ${PING_HOST} > /dev/null 2>&1
else
    ping -I ${FAILD_PRIMARY_ADDRESS} -c 1 ${PING_HOST} > /dev/null 2>&1
fi

