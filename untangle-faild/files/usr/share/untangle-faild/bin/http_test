#!/bin/dash

URL=$1

NUM_TRIES=3
TIMEOUT=$(( ${FAILD_TIMEOUT_MS} / ( ${NUM_TRIES} * 1000 )  ))
if [ "${TIMEOUT}" = "0" ]; then 
    TIMEOUT=1
fi

wget --header="FailD-Flag: true" -T ${TIMEOUT} --tries=${NUM_TRIES} -O /dev/null ${URL} 2>/dev/null

